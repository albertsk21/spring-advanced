spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/student_db?allowPublicKeyRetrieval=true&useSSL=false&createDatabaseIfNotExist=true"
    password: Albert.nustiu.2002
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQL8Dialect
    properties:
      hibernate:
        format_sql: true
    show-sql: true